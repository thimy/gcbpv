<%= form_with(model: subscription_group, url: compte_subscription_groups_path) do |form| %>
  <% if subscription_group.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(subscription_group.errors.count, "error") %> prohibited this subscription_group from being saved:</h2>

      <ul>
        <% subscription_group.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
