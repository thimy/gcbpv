<% content_for(:page_class, "events ") %>
<% content_for(:page_title, "Événements") %>

<section class="flow">
  <h2><%= inline_svg "icons/calendar-check" %>Les événements à venir</h2>
    <% if @upcoming_events.present? %>
      <%= render Website::Card::Event::Component.with_collection(@upcoming_events, inline: true) %>
    <% else %>
      <div class="panel">
        Aucun événement à venir
      </div>
    <% end %>
  <h2><%= inline_svg "icons/calendar-off" %>Les événements passés</h2>
  <div class="flow">
    <% if @passed_events.present? %>
      <%= render Website::Card::Event::Component.with_collection(@passed_events, inline: true) %>
    <% else %>
      Aucun événement passé
    <%end %>
  </div>
</section>
