<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:page_title) || "Groupement Culturel Breton des Pays de Vilaine" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="robots" content="noindex, nofollow" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="profs-space wrapper <%= "#{controller.view_context.controller_name} #{content_for(:page_class)}" %>">
    <main class="feature flow">
      <% if notice.present? %><p class="banner" data-banner-variant="success"><%= notice %></p><% end %>
      <header class="panel flow">
        <div class="repel">
          <div class="cluster">
            <div class="profile-picture"><%= image_tag @teacher.photo %></div>
            <h1>Espace professeurs</h1>
          </div>
          <%= render Shared::MenuSelect::Component.new(title: @season.name, links: @season_links) %>
        </div>
        <nav class="cluster">
          <%= link_to "Cours et ateliers", profs_path %>
          <%= link_to "Mes informations", profs_profil_path %>
        </nav>
      </header>
      <%= content_for?(:content) ? yield(:content) : yield %>
    </main>
  </body>
</html>
