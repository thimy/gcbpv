<div class="repel">
  <h1><%= @workshop.name %></h1>

  <div>
    <%= link_to "Créer un nouvel atelier", new_kid_workshop_path, class: "button" %>
    <%= link_to "Éditer cet atelier", edit_kid_workshop_path(@workshop), class: "button" %>
    <%= link_to "Retour aux ateliers", kid_workshops_path, class: "button" %>
  </div>
</div>

<div class="page-info">
  <dl class="cluster panel">
    <div>
      <dt>Nombre d’élèves :</dt>
      <dd><%= "#{@confirmed_count} (+#{@optional_count})" %></dd>
    </div>
    <div>
      <dt>Statut :</dt>
      <dd><%= @workshop.status %></dd>
    </div>
    <div>
      <dt>Notes :</dt>
      <dd><%= @workshop.comment %></dd>
  </dl>
</div>

<%= render @workshop %>
