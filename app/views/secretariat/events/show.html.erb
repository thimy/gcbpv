<div class="content-header">
  <h1><%= @event.name %></h1>

  <div>
    <%= link_to "Éditer cet événement", edit_secretariat_event_path(@event) %> |
    <%= link_to "Retour à la liste des événements", secretariat_events_path %>
  </div>
</div>

<p style="color: green"><%= notice %></p>
<%= render @event %>
